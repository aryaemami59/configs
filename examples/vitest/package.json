{
  "name": "vitest-config-example",
  "private": true,
  "scripts": {
    "test": "vitest && vitest -c vitest.config.ts && vitest -c vitest.config.cts && vitest -c vitest.config.mts"
  },
  "devDependencies": {
    "@aryaemami59/vitest-config": "workspace:^",
    "jsdom": "^24.0.0",
    "vitest": "^1.5.2"
  }
}
