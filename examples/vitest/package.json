{
  "name": "vitest-config-example",
  "private": true,
  "type": "commonjs",
  "files": [],
  "scripts": {
    "test": "vitest && vitest -c vitest.config.ts && vitest -c vitest.config.cts && vitest -c vitest.config.mts && vitest -c vitest.config.js",
    "clean": "rimraf dist",
    "test-types": "yarn clean && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@aryaemami59/vitest-config": "workspace:^",
    "jsdom": "^25.0.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "version": "",
  "sideEffects": false
}
